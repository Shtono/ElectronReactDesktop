import { SxProps } from '@mui/material'

const navLink = {
  textDecoration: 'none',
  color: 'inherit',
  cursor: 'default',
}

const text: SxProps = {
  color: '#fff',
}

export default { navLink, text }
